{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main RetroHydra window. All filesystem, network, and process work is handled by validated Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
